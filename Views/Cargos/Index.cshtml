@model IEnumerable<GestaoTarefas_CP.Models.Cargo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_Cargo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Escola_Cargo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Funcionario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_Cargo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Escola_Cargo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Funcionario.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CargoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CargoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CargoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
